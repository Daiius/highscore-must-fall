# DB 接続（雛形）。実体は .env.database にコピーして使う（.env.database はコミットしない）。
MYSQL_DATABASE=highscore
MYSQL_USER=highscore
MYSQL_PASSWORD=change-me-local-only
MYSQL_ROOT_PASSWORD=change-me-local-only
# server からの接続先（compose 内ではサービス名 db）
DB_HOST=db
DB_PORT=3306
