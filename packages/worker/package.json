{
  "name": "worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "(Phase3) サーバ側 LLM 全自動分析のジョブ処理。MVP では非実装スキャフォルド。",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "database": "workspace:*",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
